#include <iostream>
#include <algorithm>
#include <string>
using namespace std;

bool Marry(string s1, string s2)
{
	int num = min(s1.size()), s2.size());

}

int main()
{
	string s1, s2;
	cin >> s1 >> s2;
	return 0;
}